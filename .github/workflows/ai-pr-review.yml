# Disabled on 2026-09-03 after migrating automatic PR review to CodeRabbit.
# Historical implementation remains available in git history before this commit.
name: Mira Mobile OpenCode PR Review (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: OpenCode reviewer disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: |
          echo 'Automatic OpenCode PR review is disabled.'
          echo 'Mira Mobile PR review has migrated to CodeRabbit via .coderabbit.yaml.'
