# AGENTS.md

本文件是 `uichat-mira-mobile` 仓库中编码智能体的根级工作约束。

任何智能体在创建、修改或删除代码前，都必须先阅读本文件。若子目录中以后出现更具体的 `AGENT.md`，子目录规则可以补充本文件，但不得绕过这里定义的产品边界、安全要求、分支流程和验收标准。

## 1. 项目定位

`uichat-mira-mobile` 是 Mira 的官方移动端工程。

移动端不是桌面端的缩小复制品，也不是独立重写 Mira 全部能力的新系统。它首先是用户随身连接 Mira Host 的入口，负责移动交互、设备能力和可靠连接。

当前仓库处于基建阶段。首要目标是建立稳定、可维护、可验证的移动端骨架，而不是快速堆叠大量页面和功能。

## 2. 职责边界

### 移动端负责

- 应用启动、路由、页面壳和生命周期管理。
- 网络层、连接状态、超时、重试、断线恢复与错误呈现。
- Mira Host 的发现、地址配置、配对、认证和取消授权。
- 会话列表、会话打开、消息展示、输入与流式响应呈现。
- 必要的本地缓存、离线状态和安全凭据存储。
- 相机、相册、文件选择、通知、分享等移动设备能力。
- Android 与 iOS 的平台适配，以及移动端构建、测试和发布基建。

### Mira Host / 主工程负责

- Provider、模型与密钥管理。
- Agent Loop、Planner、Harness、Tool、Skill 和审批执行。
- 知识库、RAG、服务端会话状态与业务事实来源。
- 统一消息协议、Agent 状态、Artifact 和工具调用结果。
- 权限判定及其他不应由移动端自行推断的业务规则。

### 禁止越界

- 不在移动端复制 Provider 或 Agent 执行系统。
- 不在客户端伪造工具执行、审批结果或服务端状态。
- 不凭想象定义并直接绑定尚未确认的 Mira API。
- 不把认证令牌、模型密钥或敏感配置写入源码。

## 3. 第一阶段交付目标

优先打通以下最小闭环：

1. 用户填写或发现 Mira Host。
2. 移动端与 Host 完成配对和认证。
3. 用户能够查看并打开会话。
4. 用户能够发送消息并接收流式响应。
5. 网络切换、应用进入后台或短暂断线后可以恢复连接。
6. 失败时给出明确、可操作的错误信息。
7. 用户可以主动断开连接并清除本地凭据。

在这条闭环稳定前，不优先实现复杂后台设置、Provider 管理、知识库管理、角色编辑、完整工具面板或桌面端全部功能。

## 4. 协议优先

移动端与 Mira Host 的协议是两个仓库之间最重要的契约。

- 先查找仓库内已有协议、类型、Issue 或架构说明，再写网络代码。
- 不得根据 UI 需求猜测 endpoint、字段、事件名或错误码。
- 协议尚未确定时，先建立可替换的 Adapter 接口并明确标注待确认项。
- 页面组件不得散落调用 HTTP、WebSocket 或 SSE；统一通过客户端层访问。
- 外部协议数据进入应用后必须经过解析和校验。
- Mock 数据必须集中管理并明确切换，不得混入真实连接路径。
- 协议发生破坏性变更时，必须说明兼容范围和迁移方式。

## 5. 技术栈决策

仓库尚未确认的技术栈，不允许由编码智能体擅自拍板。

在生成大规模脚手架前，应由维护者确认并记录：

- 原生、React Native、Expo、Flutter、uni-app 或其他方案。
- 最低 Android / iOS 版本。
- 状态管理、路由、网络与本地数据库方案。
- 是否需要 Web 兼容。
- 构建、签名、更新和发布渠道。

技术选型优先考虑长期维护、依赖克制、国内网络环境下可稳定安装构建、便于测试，以及可随 Mira 协议演进。若使用 TypeScript，必须开启严格类型检查，不以大量 `any`、非空断言或强制类型转换绕过问题。

## 6. 安全要求

- Token、配对凭据和刷新凭据必须使用平台安全存储。
- 日志、错误上报、截图和调试面板不得泄露凭据或完整敏感消息。
- 生产连接默认使用安全传输；局域网明文连接只能作为明确标识的开发模式。
- 不自动信任同一局域网内发现的任意设备。
- 权限申请按需触发并解释用途。
- 不提交 `.env`、签名文件、证书、私钥、真实账号、构建产物或本机路径。

## 7. 编码原则

- 只解决当前明确问题，不做无关重构。
- 优先小步提交，保证每一步都能构建或独立审查。
- 避免过早抽象，只有真实重复或明确扩展点才提取通用层。
- UI 保持简洁、克制、可触达，不引入笨重的后台管理式设计。
- 业务状态、网络状态和临时 UI 状态分开管理。
- 错误不得只 `console.log` 后吞掉。
- 不静默修改锁文件、CI、构建系统或原生工程配置。
- 引入依赖前说明用途，优先选择维护活跃、体积合理、许可证兼容的方案。
- 删除或替换已有实现前，先确认其兼容、平台或测试职责。

代码标识、协议字段和提交信息使用英文；协作文档可以使用中文。注释解释“为什么”，不要重复代码本身。

## 8. 智能体工作流程

每次任务按以下顺序执行：

1. 阅读 `AGENT.md`、`README.md`、相关文档、Issue 和目标文件。
2. 检查当前分支、工作区状态和现有实现，避免覆盖他人改动。
3. 说明目标、影响范围、已知约束和仍需确认的假设。
4. 先完成最小可验证改动，再考虑扩展。
5. 执行仓库已有的格式化、类型检查、测试和构建命令。
6. 检查 diff，移除调试代码、无关格式变化和意外生成文件。
7. 汇报修改内容、验证结果、风险和未完成事项。

命令失败时不得伪称通过。必须区分代码问题、本机环境问题、缺少账号或证书，以及尚未确定的 Mira Host 协议。

## 9. 禁止的智能体行为

- 未读现有代码就整仓重写。
- 为展示“完成度”生成大量不可运行的占位页面。
- 虚构 API、测试结果、设备验证结果或构建成功状态。
- 将所有逻辑塞进单个页面、Store 或网络文件。
- 为简单需求引入庞大框架或不必要的基础设施。
- 通过禁用类型检查、Lint、测试或安全校验让构建表面通过。
- 擅自改变产品定位、仓库权限、发布流程或主工程契约。
- 未经维护者明确要求，直接修改 Mira 桌面端或服务端仓库。

## 10. 分支与发布流程

仓库的目标晋级流程如下：

```text
feature/* -> dev -> test -> prod
```

当前远程 `main`、`dev`、`test`、`prod` 尚未形成可验证的共同提交基线。维护者完成分支初始化并明确宣布启用前，本节作为目标流程，不强制现有过渡 PR 改写 base。

过渡期内：

- 智能体不得自行创建、重建、删除或强推环境分支。
- PR 必须指向维护者明确指定且具有正确历史关系的实际分支。
- 不得为了表面符合文档而复制提交、制造无关合并或改写历史。

目标流程启用后，不得跳级、逆向合并，或在多个环境分支中分别手工重复同一改动。

### `feature/*`

- 目标流程启用后，所有功能、修复、文档和基础设施改动都从最新 `dev` 创建工作分支。
- 推荐命名：`feature/<topic>`、`fix/<topic>`、`docs/<topic>`、`chore/<topic>`。
- 目标流程启用后，工作分支只向 `dev` 提交 Pull Request。
- 编码智能体不得直接把工作分支 PR 指向 `test` 或 `prod`。

### `dev`

- 日常集成分支，接收经过自检的工作分支。
- 可以包含尚待联调的功能，但必须保持可构建，并明确记录已知问题。
- 不在 `dev` 上长期直接施工；新的修改仍应创建工作分支。
- 进入下一阶段时，由 `dev` 向 `test` 提交晋级 PR。

### `test`

- 测试和候选发布分支，只接收来自 `dev` 的晋级 PR。
- 用于集成测试、设备验证、回归测试和 Mira Host 联调。
- 测试发现问题后，应从最新 `dev` 创建 `fix/*` 分支修复，再重新走 `dev -> test`。
- 不允许仅在 `test` 上形成无法回流到 `dev` 的孤立修复。

### `prod`

- 受保护的生产分支，只接收来自 `test` 的晋级 PR。
- 不直接推送、不直接开发、不跳过测试环境合并功能。
- 合并前必须确认测试结果、已知风险、协议兼容性和必要的发布说明。
- 未经维护者明确宣布紧急流程，不允许直接在 `prod` 制作 hotfix。

### 同步原则

- 文档、协议、CI 和配置变更与代码使用同一晋级流程。
- 一次晋级应尽量传递相同提交，不在三个环境分支手工复制内容。
- `AGENT.md` 等根级协作约束必须在 `dev`、`test`、`prod` 保持一致。
- 合并后应回读目标分支，确认文件内容和提交确实生效。

### 提交与 PR

- 一个提交只承担一个清晰目的。
- 提交信息应说明实际变化，例如：
  - `chore: initialize mobile workspace`
  - `feat: add host pairing flow`
  - `fix: resume stream after reconnect`
  - `docs: define mobile branch workflow`
- PR 必须包含目标、主要改动、验证方式、截图或录屏、已知风险和协议影响。
- 晋级 PR 必须明确来源和目标，例如 `dev -> test`、`test -> prod`。

## 11. 完成定义

任务只有满足以下条件才可称为完成：

- 实现符合当前任务和本文件的职责边界。
- 相关类型检查、测试和构建已经执行并如实记录结果。
- 关键失败路径得到处理。
- Android / iOS 影响已说明；无法实机验证时明确写出。
- 没有提交秘密、本地配置、调试残留或无关生成文件。
- 协议、行为或工程结构变化时，相应文档已经更新。
- 目标流程启用后，修改已按 `feature/* -> dev -> test -> prod` 流程到达任务要求的目标环境。
- 维护者可以根据说明复现验证过程。

## 12. 基建阶段的默认判断

需求不明确时，默认选择：

- 稳定连接优先于页面数量。
- 协议清晰优先于快速硬编码。
- 可验证优先于“看起来完成”。
- 小而清晰优先于复杂而通用。
- 移动端能力优先于复制桌面端能力。

遇到可能影响 Mira 主工程协议、认证模型或产品边界的决定，停止扩张实现，记录具体问题并交由维护者确认。
