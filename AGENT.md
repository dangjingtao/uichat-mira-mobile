# AGENT.md

本文件是 `uichat-mira-mobile` 仓库中编码智能体的根级工作约束。

任何智能体在创建、修改或删除代码前，都必须先阅读本文件。若子目录中以后出现更具体的 `AGENT.md`，则子目录规则可补充本文件，但不得绕过这里定义的产品边界、安全要求和验收标准。

## 1. 项目定位

`uichat-mira-mobile` 是 Mira 的官方移动端工程。

移动端不是桌面端的缩小复制品，也不是一个独立重写 Mira 全部能力的新系统。它首先是用户随身连接 Mira Host 的入口，负责移动交互、设备能力和可靠连接。

当前仓库处于基建阶段。首要目标是建立稳定、可维护、可验证的移动端骨架，而不是快速堆叠大量页面和功能。

## 2. 当前职责边界

### 移动端负责

- 应用启动、路由、页面壳和生命周期管理。
- 网络层、连接状态、超时、重试、断线恢复与错误呈现。
- Mira Host 的发现、地址配置、配对、认证和取消授权。
- 会话列表、会话打开、消息展示、输入与流式响应呈现。
- 必要的本地缓存、离线状态和安全凭据存储。
- 相机、相册、文件选择、通知、分享等移动设备能力。
- Android 与 iOS 的平台适配，以及移动端构建、测试和发布基建。

### Mira Host / 主工程负责

- Provider、模型与密钥管理。
- Agent Loop、Planner、Harness、Tool、Skill 和审批执行。
- 知识库、RAG、服务端会话状态与业务事实来源。
- 统一消息协议、Agent 状态、Artifact 和工具调用结果。
- 权限判定及其他不应由移动端自行推断的业务规则。

### 禁止越界

- 不在移动端复制一套 Provider 或 Agent 执行系统。
- 不在客户端伪造工具执行、审批结果或服务端状态。
- 不凭想象定义并直接绑定尚未确认的 Mira API。
- 不为了赶进度把认证令牌、模型密钥或敏感配置写进源码。

## 3. 第一阶段交付目标

优先打通以下最小闭环：

1. 用户填写或发现 Mira Host。
2. 移动端与 Host 完成配对和认证。
3. 用户能够查看并打开会话。
4. 用户能够发送消息并接收流式响应。
5. 网络切换、应用切后台或短暂断线后可以恢复连接。
6. 失败时给出明确、可操作的错误信息。
7. 用户可以主动断开连接并清除本地凭据。

在这条闭环稳定前，不优先实现复杂后台设置、Provider 管理、知识库管理、角色编辑、完整工具面板或桌面端全部功能。

## 4. 协议优先

移动端与 Mira Host 的协议是两个仓库之间最重要的契约。

编码智能体必须遵守：

- 先查找仓库内已有协议、类型、Issue 或架构说明，再写网络代码。
- 不得根据 UI 需求自行猜测 endpoint、字段、事件名或错误码。
- 协议尚未确定时，应先创建清晰的草案或 Adapter 接口，并标出待确认项。
- 页面组件不得直接散落调用 HTTP、WebSocket 或 SSE；统一通过可替换的客户端层访问。
- 外部协议数据进入应用后必须经过解析与校验，不能直接断言类型正确。
- 协议变更应记录兼容范围；破坏性变更必须明确说明迁移方式。
- Mock 数据必须集中管理，并能明确切换；不得让演示数据混入真实连接路径。

建议逐步形成以下契约层：

- `HostDiscovery`
- `PairingClient`
- `AuthSession`
- `ConversationClient`
- `StreamingTransport`
- `ArtifactClient`

名称可按最终技术栈调整，但职责边界应保持清晰。

## 5. 技术栈决策

仓库尚未确认的技术栈，不允许由编码智能体擅自拍板。

在生成大规模脚手架前，应先由维护者确认并记录：

- 原生、React Native、Expo、Flutter、uni-app 或其他方案。
- 最低 Android / iOS 版本。
- 状态管理、路由、网络与本地数据库方案。
- 是否需要 Web 兼容。
- 构建、签名、更新和发布渠道。

技术选型应优先考虑：

- 可长期维护，而不是只适合一次性演示。
- 尽量少的重量级依赖和隐式魔法。
- 国内开发网络环境下能够稳定安装和构建。
- Android 与 iOS 行为一致，同时允许必要的平台差异。
- 易于测试、替换网络层并与 Mira 协议演进。

若选择 TypeScript，必须开启严格类型检查，不以大量 `any`、非空断言或类型强转绕过问题。

## 6. 安全要求

- Token、配对凭据和刷新凭据必须使用平台安全存储。
- 日志、错误上报、截图和调试面板不得泄露凭据或完整敏感消息。
- 生产连接默认使用安全传输；局域网明文连接只能作为明确标识的开发模式。
- 认证失败、凭据撤销和 Host 身份变化必须有清晰处理。
- 不自动信任同一局域网内发现的任意设备。
- 权限申请应按需触发，并解释用途；不要在首次启动时一次性索取全部权限。
- 不提交 `.env`、签名文件、证书、私钥、真实账号、构建产物或本机路径。

## 7. 编码原则

- 先解决当前明确问题，不进行无关重构。
- 优先小步提交，保证每一步都能构建或被独立审查。
- 避免过早抽象；只有出现真实重复或明确扩展点时才提取通用层。
- UI 保持简洁、克制、可触达，不引入笨重的后台管理式设计。
- 业务状态、网络状态和临时 UI 状态应分开管理。
- 错误不能只 `console.log` 后吞掉；需要转换成用户可理解且开发者可定位的结果。
- 不静默修改锁文件、CI、构建系统或原生工程配置。
- 引入依赖前说明用途，并优先选择维护活跃、体积合理、许可证兼容的方案。
- 删除或替换已有实现前，先确认它是否承担兼容、平台或测试职责。

代码标识、协议字段和提交信息使用英文；面向协作者的说明文档可以使用中文。注释应解释“为什么”，不要重复代码本身。

## 8. 智能体工作流程

每次任务按以下顺序执行：

1. 阅读 `AGENT.md`、`README.md`、相关文档、Issue 和目标文件。
2. 检查当前分支、工作区状态和现有实现，避免覆盖他人改动。
3. 用几句话说明目标、影响范围、已知约束和仍需确认的假设。
4. 先完成最小可验证改动，再考虑扩展。
5. 执行仓库已有的格式化、类型检查、测试和构建命令。
6. 检查 diff，移除调试代码、无关格式变化和意外生成文件。
7. 汇报修改内容、验证结果、风险以及尚未完成的事项。

若命令失败，不得伪称通过。应保留关键错误信息，并区分：

- 代码问题；
- 本机环境或依赖问题；
- 缺少账号、证书或外部服务；
- 尚未确定的 Mira Host 协议。

## 9. 禁止的智能体行为

- 未读现有代码就整仓重写。
- 为展示“完成度”而生成大量不可运行的占位页面。
- 虚构 API、测试结果、设备验证结果或构建成功状态。
- 将所有逻辑塞进单个页面、Store 或网络文件。
- 为简单需求引入庞大框架、复杂代码生成器或不必要的基础设施。
- 通过禁用类型检查、Lint、测试或安全校验让构建表面通过。
- 擅自改变产品定位、仓库权限、发布流程或主工程契约。
- 未经维护者明确要求，直接修改 Mira 桌面端或服务端仓库。

## 10. 分支与提交

- `main` 应保持可构建、可运行或至少处于明确的基建可验证状态。
- 功能开发优先使用 `feat/<topic>`，修复使用 `fix/<topic>`，基础设施使用 `chore/<topic>`。
- 一个提交只承担一个清晰目的。
- 提交信息应说明实际变化，例如：
  - `chore: initialize mobile workspace`
  - `feat: add host pairing flow`
  - `fix: resume stream after reconnect`
  - `docs: define mobile host protocol`
- Pull Request 应包含目标、主要改动、验证方式、截图或录屏、已知风险和协议影响。

## 11. 完成定义

任务只有在以下条件满足时才可称为完成：

- 实现符合当前任务和本文件的职责边界。
- 相关类型检查、测试和构建已执行并如实记录结果。
- 关键失败路径得到处理，而不只有理想路径。
- Android / iOS 影响已说明；无法实机验证时明确写出。
- 没有提交秘密、本地配置、调试残留或无关生成文件。
- 协议、行为或工程结构发生变化时，相应文档已更新。
- 维护者可以根据说明复现验证过程。

## 12. 基建阶段的默认判断

当需求不明确时，默认选择：

- 稳定连接优先于页面数量。
- 协议清晰优先于快速硬编码。
- 可验证优先于“看起来完成”。
- 小而清晰优先于复杂而通用。
- 移动端能力优先于复制桌面端能力。

遇到可能影响 Mira 主工程协议、认证模型或产品边界的决定，停止扩张实现，记录具体问题并交由维护者确认。