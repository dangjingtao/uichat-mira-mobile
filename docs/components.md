# 组件规范

## 会话列表项

```
┌─────────────────────────────────────┐
│ ┌───┐  通用助手            5分钟前  │
│ │ G │  点击开始聊天...               │
│ └───┘                               │
└─────────────────────────────────────┘
```

- 头像：48x48px，圆角 8px，背景 Primary
- 标题：16px，Semibold，单行截断
- 时间：12px，Muted
- 预览：14px，Muted，单行截断
- 分隔线：左边距 76px，高度 hairline

## 聊天气泡

### 用户消息
- 背景：Primary (`#6366f1`)
- 文字：白色，15px
- 位置：右侧
- 形状：圆角 16px，右下角 4px

### AI 消息
- 背景：`#f0f0f0`
- 文字：`#111111`，15px
- 位置：左侧
- 形状：圆角 16px，左下角 4px

## 输入框

```
┌─────────────────────────────────────┐
│ ┌──────────────────┐ ┌──────────┐  │
│ │ 输入消息...      │ │  发送    │  │
│ └──────────────────┘ └──────────┘  │
└─────────────────────────────────────┘
```

- 输入区：灰底 `#f5f5f5`，圆角 20px
- 发送按钮：Primary 背景，圆角 20px
- 最小高度：40px，最大高度：120px

## 按钮状态

| 状态 | 背景 | 文字 |
|-----|------|------|
| 默认 | Primary | 白色 |
| 按下 | Primary Dark | 白色 |
| 禁用 | `#c7c7c7` | 白色 |

## 配置卡片

- 背景：`#f9f9f9`
- 圆角：16px
- 内边距：20px
- 输入框：白底，边框 `#e0e0e0`，圆角 12px

## 空状态

```
┌─────────────────────────────────────┐
│                                     │
│          暂无会话                    │
│      点击下方按钮开始新对话           │
│                                     │
└─────────────────────────────────────┘
```

- 标题：18px，Semibold，`#666666`
- 副标题：14px，`#999999`
